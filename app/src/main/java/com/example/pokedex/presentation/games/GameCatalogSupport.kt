package com.example.pokedex.presentation.games

/**
 * Shared game runtime is deliberately supplied through [MiniGameHost].
 * Game-specific files must not reach into ScannerScreen or own navigation state.
 */
internal object GameCatalogSupport
